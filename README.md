# self-healing-classification-dag
A LangGraph-based transformer pipeline that performs text classification with a self-healing fallback mechanism. Automatically triggers user clarification or backup inference when model confidence is low.
